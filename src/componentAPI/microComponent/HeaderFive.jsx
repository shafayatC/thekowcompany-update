const HeaderFive = ({children, className}) => {
    return (
        <h5 className={`anek-latin-font text-[14px] lg:text-base font-bold ${className}`}>{children}</h5>
    );
};

export default HeaderFive;